{
  "private": true,
  "workspaces": [
    "packages/main",
    "packages/router",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint packages/*/src/**/*.ts shared/**/*.ts",
    "lint:fix": "eslint packages/*/src/**/*.ts shared/**/*.ts --fix",
    "test": "vitest run",
    "test:unit": "vitest run packages/main/test/unit packages/router/test/unit shared/test",
    "test:integration": "vitest run packages/main/test/integration packages/router/test/integration",
    "test:e2e": "vitest run test/e2e",
    "test:coverage": "vitest run --coverage",
    "package": "npm run build && npm run package -w remote-notifier-codex && npm run package -w remote-notifier-codex-router"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "@vitest/coverage-v8": "^4.1.5",
    "eslint": "^10.2.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-perfectionist": "^5.9.0",
    "eslint-plugin-prettier": "^5.5.5",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.0",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "esbuild": "0.28.0",
    "node-notifier": "^10.0.1"
  }
}
